package com.bashwork.commons.serialize;

/**
 * An interface that describes a utility that can serialize
 * a type to and from a java byte array.
 */
public interface ByteArraySerializer extends Serializer<byte[]> {
}
